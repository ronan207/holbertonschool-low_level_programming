#include <stdio.h>

/**
 * main - Entry point
 * @argc: is the fisrt varibale
 * @argv: is th secound variable
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc - 1);
	return (0);
}
