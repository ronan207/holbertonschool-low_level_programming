#include <stdio.h>

/**
 * main - Entry point
 * puts - print "with proper grammar, but the outcome is a piece of art"
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
