#include "main.h"

/**
 * print_sign - Write a function that prints the sign of a number
 *
 * @c: is my variable
 * Return: 1 0
 */

int _isdigit(int c);
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	return (0);
}
