#include"main.h"

/**
 *
 * int _abs - Write a function that computes the absolute value of an integer
 *
 * Return: 0
 */

int _abs(int x)
{
	if (x < 0)
	{
		return (-x);
	}
	else
	{
		return (x);
	}
}
