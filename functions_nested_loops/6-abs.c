#include"main.h"

/**
 * _abs - Write a function that computes the absolute value of an integer
 * @x : is my variable
 * Return: 0 Succes
 */

int _abs(int x)
{
	if (x < 0)
	{
		return (-x);
	}
	else
	{
		return (x);
	}
}
